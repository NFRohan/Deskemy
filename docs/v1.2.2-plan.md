# Deskemy v1.2.2 — plan

**Status: IMPLEMENTED — not yet released.** Both code items and the README ask
are done and tested; awaiting a version bump + build. See
[ROADMAP.md](../ROADMAP.md) for the broader plan.

Legend: ✅ done · 🔜 next · ⏳ planned · ❌ cut

---

## Queued

### ✅ Keep the system awake during playback (GH #3)

**Problem.** Watching a long lecture with no keyboard/mouse input lets Windows
sleep (and blank the display), interrupting study. Nothing in the backend
inhibits sleep or the screensaver today — verified, there is no
`SetThreadExecutionState` call anywhere.

**Why mpv doesn't cover it.** mpv's `stop-screensaver` defaults to on, but it
works *through the video output's own window*. Our default compositor path sets
`vo=libmpv` (`src-tauri/src/player/mod.rs`), where mpv renders into a texture we
own and has no window — so mpv can't inhibit anything and the host app must.
(The `wid` fallback has a child HWND and may partially work, which is why this
can look inconsistent between machines.)

**Fix.** While playing, assert
`SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED | ES_DISPLAY_REQUIRED)`;
on pause / stop / EOF / app exit, drop back to plain `ES_CONTINUOUS`.

- No new dependency — we already use `windows-sys`; just add the
  `Win32_System_Power` feature.
- **Per-thread caveat:** the execution state is scoped to the calling *thread*,
  so it must be asserted from a long-lived thread (the player's event pump), not
  a transient one, or it lapses when that thread exits.
- Tie it to the existing play/pause state transitions so it releases correctly —
  don't leave the machine pinned awake after the app is closed or paused.
- Non-Windows: no-op behind `cfg(windows)` (see [linux-port-decision] context).

**Testing:** set a short sleep timer in Windows power settings, play a video,
confirm the machine stays awake; pause and confirm it sleeps normally; close the
app mid-playback and confirm the inhibit is released.

### ✅ Clean up leftover auto-update installers in %TEMP%

**Problem.** After every installer-path auto-update, the downloaded NSIS
installer (~37 MB) is left behind in the user's temp dir. It's an upstream
`tauri-plugin-updater` behavior (verified in 2.10.1), not our code:

- `downloadAndInstall()` writes `Deskemy_<ver>_x64-setup.exe` to
  `%TEMP%\Deskemy-<ver>-updater-<rand>\Deskemy-<ver>-installer.exe`.
- The temp dir is created with `.keep()` (opts out of auto-delete).
- The installer file is wrapped in a `tempfile::TempPath` (delete-on-drop),
  BUT `install_inner` launches the installer via `ShellExecuteW` and then calls
  `std::process::exit(0)` — which does NOT run destructors, so the cleanup guard
  never fires. The `process::exit` is necessary (release file locks so NSIS can
  overwrite the app), so this can't be fixed from our call site.

Result: one ~37 MB installer + its `Deskemy-<ver>-updater-*` folder leaks per
update. Low severity (it's in %TEMP%; Storage Sense eventually reclaims it) but
real, and compounds across releases. The **portable** path is clean (it only
opens the release page — no Deskemy-managed download).

**Fix.** A cheap startup sweep, mirroring the existing startup auto-reconcile
thread in `src-tauri/src/lib.rs` (the `library:changed` one):

- On launch, off the main thread, scan `std::env::temp_dir()` for
  `Deskemy-*-updater-*` directories and best-effort remove them (ignore errors
  from locked files).
- **Safe** because by the time the sweep runs the app IS the post-update
  version, and the installer that spawned it has essentially finished — those
  dirs are stale. Any file still locked (rare race: installer exe not yet
  exited) just fails silently and gets swept on the next launch.
- ~15 lines. Scope the glob to our app prefix so we never touch unrelated temp
  files. No dependency on the plugin internals beyond the well-known prefix.

**Testing:** light — trigger an update (or drop a dummy `Deskemy-x-updater-*`
dir in %TEMP%), relaunch, confirm it's gone; confirm a locked file is skipped
without erroring.

---

## Candidate / accumulating (from user feedback)

### ✅ Link the community Chinese translation in the README (GH #4)

Not release-gated — a README edit can land any time, independent of this patch.
[@flipped0419](https://github.com/flipped0419) maintains a Simplified Chinese
translation at <https://github.com/flipped0419/Deskemy-zh-CN> and asked only for
a README mention (he explicitly did *not* ask for i18n to be merged). Frame it as
a **community translation** — credited, warm, and noting it's maintained
separately so it may lag behind a release.

**Not committing to real i18n.** If Chinese demand keeps growing, the actual fix
is a message catalog + language setting, but that's every UI string across the
`.svelte` files plus Rust error messages — a real chunk of work, deliberately not
promised.

_(Add further feedback-driven items here, then size the 1.2.2 batch.)_
